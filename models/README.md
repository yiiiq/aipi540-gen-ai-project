# Models

After training, the LoRA adapter should be saved to:

```text
models/medexplain-lora/
```

For the hackathon submission, include the trained adapter in the repository if it is small enough for GitHub, or upload it to Hugging Face and set `MEDEXPLAIN_MODEL_DIR` to that adapter location in deployment.
